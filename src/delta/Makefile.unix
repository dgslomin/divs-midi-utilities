
ifeq ("$(shell uname -s)", "Darwin")
BINARY=delta.app
else
BINARY=delta
endif

default:
	test -d target || mkdir target
	cd target && qmake ..
	cd target && make
	rm -rf ../../bin/$(BINARY)
	cp -r target/$(BINARY) ../../bin

clean:
	rm -rf target

reallyclean: clean
	rm -rf ../../bin/$(BINARY)

